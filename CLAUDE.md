# CLAUDE.md

## 项目

String Lab（网球穿线助手）。栈、命令、数据文件说明见 `README.md`，这里只记 README 没写、但影响判断的部分：

- **属性分是编辑估值，不是实验室数据。** `strings.ts` / `rackets.ts` 里的 0–100 来自公开评测的归纳。模型输出用来比较**相对差异**（A 比 B 舒适多少）是可靠的，当绝对值用不可靠。
- **两个时钟。** `src/state/bag.ts` 的 `PLAY_HOURS_LIMIT`（聚酯 15h / 多丝 30h）和 `CALENDAR_DAYS_LIMIT`（聚酯 90d / 多丝 180d）是独立的：前者管磨损，后者管聚酯在拍袋里自己变硬。低频球员通常是日历先到期。
- 改 `src/data/*.ts` 后跑 `npm test`，完整性测试和 i18n 对齐测试会兜住漏字段。
- 交流语言：中文。

## 项目所有者画像

> 这是**仓库所有者本人**的球员画像，用于回答他的选拍/选线问题。
> **不要**把它当成 app 的默认用户或用它来调 app 的默认值——app 面向的是所有人。

### 基本情况

| 项 | 值 |
|---|---|
| 水平 | NTRP 3.5–4.0（自评） |
| 场景 | 不打比赛。和太太、朋友休闲对拉 |
| 打法 | **不是纯上旋**。平击 / 上旋 / 切削都有，会时不时发力 |
| 强度 | 偏低，拉球为主 |
| 每周球时 | **未确认**（换线周期的结论对这个数很敏感，需要时先问） |

### 装备

| | 拍 | 线 | 磅数 |
|---|---|---|---|
| 现役 | Wilson Clash 98 v2（16×20, RA 55, 310g） | Babolat RPM Blast 1.25 | ~52/50（**未确认**） |
| 即将更换 | Wilson Pro Staff 97 v14（16×19, RA 66, 315g） | Tecnifibre Triax 1.28 | 56/54 |

现役那副 Blast 竖线凹槽已达约**半个线径**：线床是**机械锁死**（卡在凹槽里，snapback 没了），不是掉磅问题。它的实际表现远低于目录值，**不能拿来当"聚酯是什么感觉"的参照**。

### 解读这个画像的规则

这几条是反复推翻错误假设后定下来的，评估任何配置前先过一遍：

1. **旋转这一项对他权重很低。** 他没有在兑现高旋转线的数值。排序时优先看 **舒适 / 保磅 / 力量**，旋转掉 40+ 分对他基本无感。
2. **聚酯对他是错误品类。** 球时低 → 聚酯的 90 天日历上限先于打球小时数触顶 → 每副线有一大半寿命在拍袋里自己耗掉。多丝 / 复合线（如 Triax）才对。
3. **他打不断线。** 没有"断线"这个换线触发器。默认假设他的线床已经超期，除非有明确的穿线日期。
4. **别套用"力量高会出底线"的常规警告。** 那条前提是快速挥拍的高旋转打法。他挥拍速度不高，而 Pro Staff 97 是低力量框（配 Blast 只有 50 分），需要线来补力量。对他来说高力量是收益不是风险。
5. **凹槽深度 ≠ 打法强度。** 它只反映"累计小时 × 任意程度的上旋"。强度低只意味着磨到那个深度花了更久，反推不出打法。
6. **不要推荐混穿。** 混穿的价值在于找回旋转，而他不用旋转，却要为此多付一根 15 小时就死的聚酯 + 一道工序。

### 数值基线

`computeBed` 算的，省得重复推导（力量/控制/旋转/舒适/耐久/保磅）：

| 配置 | 有效磅 | 力量 | 控制 | 旋转 | 舒适 | 耐久 | 保磅 |
|---|---|---|---|---|---|---|---|
| Clash 98 + Blast 52/50（理论新线） | 51.2 | 43 | 82 | 89 | 48 | 68 | 43 |
| **PS97 + Triax 56/54（计划）** | 55.2 | 82 | 65 | 44 | 84 | 62 | 70 |
| PS97 + Triax 54/52（觉得费劲就降到这） | 53.2 | 85 | 62 | 45 | 86 | 62 | 69 |
| Clash 98 + Triax 56/54（旧拍留作副拍） | 55.2 | 75 | 67 | 40 | 90 | 62 | 70 |

换到 PS97 + Triax 的净变化：舒适 **+36**、保磅 **+27**、力量 **+39**、控制 −17、旋转 −45。

**已知风险不在线上，在拍框**：PS97 v14 是 97 拍面、9 点头轻、RA 66，甜区小、偏心球容错差，而 `computeBed` 不建模容错性。从 Clash 98 换过去最可能不适应的是这个。

### 待确认

- 每周球时（决定换线周期）
- 现役 Clash 的实际磅数
- Clash 98 是否保留作副拍
